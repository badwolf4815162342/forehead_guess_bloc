name: decks_repository
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  decks_api:
    path: ../decks_api

dev_dependencies:
  mocktail: ^0.2.0
